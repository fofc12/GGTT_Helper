<Window x:Class="ZeroWubiLens.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="零西五笔工坊 · 拆字镜"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="False"
        SizeToContent="Manual"
        Width="330" Height="134"
        Background="#FF20232A"
        UseLayoutRounding="True"
        FontFamily="Microsoft YaHei UI, Segoe UI">
    <Border BorderBrush="#FF3A3F4B" BorderThickness="1" Padding="10,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <TextBlock x:Name="SourceLabel" DockPanel.Dock="Right"
                           Foreground="#FF8A91A0" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock x:Name="CharText" Text="" Foreground="White"
                           FontSize="30" FontWeight="SemiBold"/>
            </DockPanel>

            <StackPanel Grid.Row="1" Margin="0,6,0,2">
                <StackPanel x:Name="KeyChips" Orientation="Horizontal"/>
                <TextBlock x:Name="MnemonicText" Foreground="#FFAAAABB" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel x:Name="CodePanel" Grid.Row="2" Orientation="Horizontal" Margin="0,6,0,2"/>

            <TextBlock Grid.Row="3" x:Name="FooterText"
                       Text="再按 Alt+Z 看拆字图 · 松开 Alt 关闭"
                       Foreground="#FF6A7080" FontSize="11" Margin="0,6,0,0"/>

            <Border Grid.Row="4" x:Name="WebHost" Margin="0,8,0,0"
                    Visibility="Collapsed" Background="White">
                <Grid>
                    <wv2:WebView2 x:Name="Web"/>
                    <TextBlock x:Name="WebHint" Text="拆字图加载中…" Foreground="#FF6A7080"
                               FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
